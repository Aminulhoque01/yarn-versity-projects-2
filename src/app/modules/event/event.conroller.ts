// import { NextFunction, Request, Response } from "express";


// const createEvent = async(req:Request, res:Response, next:NextFunction):Promise<void>=>{
//     const {eventID, eventName, eventDate, eventLocation, participants}= req.body;

//     const event = new Event({ eventID, eventName, eventDate, eventLocation, participants });
//       await event.save();


// }